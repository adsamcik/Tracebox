<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-sdk android:minSdkVersion="30" />

    <application>
        <activity
            android:name=".TraceboxDisclosureActivity"
            android:exported="false" />
        <provider
            android:name=".TraceboxFileProvider"
            android:authorities="${applicationId}.tracebox.export"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/tracebox_export_paths" />
        </provider>
    </application>
</manifest>
