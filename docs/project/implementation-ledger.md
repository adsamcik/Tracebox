# Tracebox Persistent Implementation Ledger

- Immutable baseline SHA: `dc87c6f9e2a6576cc554f7cb181ce80a02bf0802`
- Implementation history: `copilot/tracebox-foundation`
- Active requirements branch: `codex/personal-project-scope`
- Active checkout: `G:\Github\Tracebox`
- Scope currently authorized: Phase 0-5 under ADR-0008; API 23 and the single-emulator lane under ADR-0009; personal-project completion, consolidated fixtures, and Tracker integration under ADR-0010
- Allowed states: `NOT_STARTED`, `IN_PROGRESS`, `PASS`, `FAIL`, `BLOCKED_PRODUCT_DECISION`, `UNAVAILABLE_EXTERNAL`, `NOT_APPLICABLE_WITH_RATIONALE`
- Commit rule: `satisfied-by-prior-commit` is filled only by a later commit; a row never records its containing commit.

## Dependency-aware work packages

| ID | Work | Dependencies | Acceptance/output | State | Satisfied by prior commit | Notes/evidence |
|---|---|---|---|---|---|---|
| F0.1 | Freeze terminology and invariants | — | Threat model, privacy classes, evidence grades, readiness states approved | PASS | 76c57c4482ead7e45c900a170014761b692fd8fd | Frozen by ADR-0002 through ADR-0007 and `specs/`. |
| F0.2 | Pin toolchains and dependencies | F0.1 | Gradle, AGP, Kotlin, JDK, NDK, CMake, Rust, Crashpad revisions locked and verified | PASS | 4e7959b70097426b253dda3671eef4a8b16ae1d9 | Crashpad reuse rehashes the complete expected post-patch source tree against a tracked lock value. Round-2 acquisition additionally authenticates every archive's locked byte size/SHA-256 before extraction and rejects absolute, drive-prefixed, traversal, link, device, and FIFO entries before destination writes. See `evidence/phase0/review-fix-round2-host-validation.json`. |
| F0.3 | Crashpad privacy spike | F0.1;F0.2 | Raw stream inventory, structural-summary prototype, seeded-secret results | IN_PROGRESS |  | API-23 x86_64 and arm64 artifacts build. The required API 36 targeted run now passes with exactly the five allowed streams, seeded raw evidence present, no seeded summary value, and no internal-identity match. Full qualification remains pending. Evidence: `evidence/phase0/API36-x86_64-4096-review-fix-qualification.json`. |
| F0.4 | Android handler spike | F0.2 | Multi-client handler on the required API 36 x86_64 4 KiB emulator, with restart/death evidence | IN_PROGRESS |  | ADR-0009 makes the existing emulator the sole required lane. The targeted API 36 deadline regression passes; the complete lifecycle/performance run remains pending. Historical API 30/API 37 failures are retained but are no longer release gates. |
| F0.5 | Emergency fallback spike | F0.2 | Fixed signal record survives pre-Durable and Crashpad-unavailable faults on the required emulator | IN_PROGRESS |  | The API 36 targeted run passes emergency restart reset, handler-unavailable fallback, exact prior-action chaining, and observable signal death. The complete required run remains pending. |
| F0.6 | Live ANR spike | F0.3;F0.4 | Functional watchdog, candidate capture, nonfatal request, lifecycle, bounded-work, timeout/cancellation, and resource smoke on the required emulator | IN_PROGRESS |  | The API 36 targeted hung-handler deadline regression passes. The consolidated functional, false-confirmation, lifecycle, and resource smoke run remains pending. Historical percentile/target values remain evidence but are advisory under ADR-0010. |
| F0.7 | Baseline artifact and resource smoke measurement | F0.3;F0.4;F0.5;F0.6 | Artifact hashes plus one-emulator app/handler resource baseline | IN_PROGRESS |  | API-23 x86_64 and arm64 Crashpad artifacts build with 16 KiB ELF alignment retained, and the API 36 targeted fixture passes. One pinned-toolchain rebuild pair, resource baseline, and no-network smoke remain pending. Cross-environment byte-identical APKs and physical-device statistics are advisory under ADR-0010. |
| C1.1 | Formal privacy/event schema | F0.1;F0.3 | Stable bounded privacy-classified schema | PASS |  | `schema/events.json` drives privacy and bounded fields. Evidence: `evidence/phase1/schema-compiler.json`. |
| C1.2 | Schema model and compiler | C1.1 | Kotlin/C/C++/Rust/protobuf/docs generation and golden tests | PASS |  | One strict model generates Kotlin recording types/functions, C structs/declarations, Rust ABI types, correct protobuf scalar types, decoder metadata, labels, and docs; drift golden test passes. Evidence: `evidence/phase1/schema-compiler.json`. |
| C1.3 | Kotlin public API | C1.2 | Generated-only recording API | PASS |  | `android/tracebox-api` accepts only schema-generated bounded values; enablement is checked before construction. Evidence: `evidence/phase1/kotlin-api-and-identity.json`. |
| C1.4 | Versioned C ABI | C1.2 | Size-prefixed ABI and compatibility tests | PASS |  | Generated C event layouts and the emergency public view use v1 headers; the Phase 0 raw 256-byte storage remains internal. MSVC builds and CTest validates emergency, ABI, generated-event linkage, and initialization compatibility. Evidence: `evidence/final/c1-native-abi-msvc-validation.json`. |
| C1.5 | Rust bindings/wrapper | C1.4 | Generated sys crate and safe wrapper | PASS |  | Exported size-prefixed `extern "C"` functions catch unwinds; prior Rust tests and the now-passing C ABI prerequisite validate the wrapper contract. Evidence: `evidence/phase1/rust-bindings-and-identities.json`, `evidence/final/c1-native-abi-msvc-validation.json`. |
| C1.6 | Identity and build contract | F0.2;C1.2 | Internal identities and build/symbol catalog | PASS |  | Opaque typed identity allocation journals before return; summaries are deterministic and journaled. The Gradle plugin captures the configured schema fingerprint plus AGP-produced R8 mapping SHA-256/derived ID and scans native ELF artifacts through public AGP artifact APIs. Current fixture libraries do not contain GNU `.note.gnu.build-id`, so the catalog records an explicit `sha256` fallback rather than claiming a GNU ELF build ID. Gradle unit, TestKit consumer, real minified build, identity capture, and release-conformance checks pass in `evidence/phase5/personal-release-host-readiness.json`. |
| R2.1 | Lifecycle/readiness runtime | C1.3 | Install and four readiness states | PASS | abab979eab6237b4593dedce951ea04a18d96b50 | Explicit volatile install, conflict/idempotency result, constrained provider fallback, and durable-after-policy transition tested. |
| R2.2 | Policy epoch protocol and local gate | F0.4;C1.1;R2.1 | Epoch revalidation and barrier protocol | PASS | abab979eab6237b4593dedce951ea04a18d96b50 | Persisted control-page seam and per-writer epoch/mask revalidation are implemented; X3.2 provides production handler ownership and global acknowledgements. |
| R2.3 | Bounded queues, barrier, health codes | R2.2 | Fixed-capacity policy-aware queues | PASS | abab979eab6237b4593dedce951ea04a18d96b50 | Fixed priority queue, local barrier purge, and recursion-free health counters are tested; X3.2 drives the production cross-process barrier. |
| R2.4 | Segment framing/recovery | C1.2;C1.6 | Valid-prefix append-only segment recovery | PASS | 1ae57abc378f352afeb6d98488aa1d702bf8543d | CRC32C framed segments recover valid prefixes and seal immutably. Segment/process IDs are injected only from a Phase 1 persist-before-use allocator bridge. |
| R2.5 | Role and UID-wide quotas/retention | R2.4 | Complete hard storage bounds | PASS |  | One production coordinator owns the UID-wide byte/file ledger across classified CE and DE roots; ordinary segments, raw/handoff artifacts, summaries, journals, indexes, staging, temporary files, emergency reserves, and preallocated Direct-Boot storage join the same cross-process mutation barrier and fail closed on partial ownership. Reconciliation, exact reservation/release, concurrent quota, eviction/progress, file-count, corruption, and restart tests pass; emulator storage pressure is validation only. |
| R2.6 | Lazy metadata summaries/index | R2.4;R2.5 | Rebuildable index and index-free planning | PASS | 1ae57abc378f352afeb6d98488aa1d702bf8543d | Direct segment scan is authoritative; bounded index rebuild/delete fallback is tested. |
| R2.7 | Direct Boot C0 module | C1.1;R2.2;R2.4 | C0-only DE store and two-phase deny mirror | PASS |  | The schema compiler emits the generated Direct-Boot record/fingerprint contract; explicit unlocked activation preallocates and quota-accounts the fixed DE C0 store; CE/DE policy uses a crash-safe pending/active fail-closed transaction; and locked-boot writes are imported into CE with bounded, durable, idempotent retirement on unlock. Focused schema, policy-boundary, quota, capacity, import, crash-recovery, and production-facade tests pass; the scripted locked-boot emulator run is validation only. |
| R2.8 | Crash-recoverable deletion engine | R2.2;R2.3;R2.4;R2.5;R2.6;R2.7 | Journaled whole-store deletion | PASS |  | Production `ALL_TRACEBOX_DATA` commits and rechecks the durable CE/DE deny, quiesces managed/native/handler/package participants, marks both claimed storage roots ineligible, and journal-resumably deletes and reconciles all classified diagnostic, import, emergency, and staging paths while preserving only fail-closed controls. Host crash-boundary, partial-failure, quota, restart, and no-resurrection tests cover the transaction. Per-record/summary selective deletion is unexposed optional post-foundation work under the personal scope; the one-emulator delete/restart run is validation only. |
| X3.1 | Handler service and IPC | F0.4;C1.4;R2.1 | One handler and bounded local IPC | PASS |  | The non-exported capture-only service starts through a one-shot durable permit and canonical socket path. Its bounded native `SOCK_SEQPACKET` protocol checks version, same-UID peer credentials, PID/role/identity/epoch, finite retry/timeout, lifecycle completion, death notification, and conclusive stale-socket recovery without an idle poll loop. Native/JVM protocol, race, malformed-input, shutdown, and socket tests pass; live handler lifecycle is emulator validation only. |
| X3.2 | Production global policy coordinator | R2.2;R2.3;R2.8;X3.1 | Control page, census, leases, global barriers | PASS |  | Production CE/DE policy changes use a durable transition journal plus the native handler's bounded registered-participant census/lifecycle channels. Prepare acknowledgements precede the local durable tuple, finalize/rollback is explicit, disconnects fence capture fail-closed, handler startup and deletion share the UID mutation barrier, and partial outcomes never claim convergence. Host policy-boundary, registration, death, restart, and malformed-protocol tests pass; real multiprocess timing is emulator validation only. |
| X3.3 | Capture-only Crashpad integration | F0.3;C1.1;C1.6;R2.5;R2.8;X3.1;X3.2 | Private capture-only Crashpad lifecycle | PASS |  | The native Crashpad bridge registers the exact raw-artifact/process/role/epoch tuple before capture, writes lifecycle state before bytes, and hands completed pending dumps into bounded app-owned storage without an uploader. Startup recovery audits the full pending/lifecycle/handoff census, rejects ambiguity and symlinks, verifies exact journal/file identity repeatedly under the mutation barrier, transfers quota atomically with rollback, expires C2 data, and deletes unverifiable orphans. Host native/storage fault tests cover the hard-death boundaries; live process-death handoff is emulator validation only. |
| X3.4 | Structural summary derivation and spool | X3.3;C1.2;C1.6;R2.5;R2.8 | Canonical summary staging and replay | PASS |  | Production handoff calls the Rust `SummaryId::derive` FFI boundary over the bounded canonical summary, stages it durably, and replays idempotently through append -> force/seal -> acknowledgement -> source retirement. Internal IDs stay out of Standard export, and crash-boundary, duplicate, mismatch, quota, and replay tests pass. Live native-fault symbol/summary observation is emulator validation only. |
| X3.5 | Emergency writer production path | F0.5;R2.5 | Signal-safe emergency capture | PASS |  | The signal-safe native emergency slot is initialized for application and handler roles, participates in the native dispatch fallback, and is consumed at startup into a generated policy-gated `EmergencyRecord`. Completed slots retire only after durable append; incomplete, corrupt, denied, and duplicate slots fail safely. Native/JVM startup and recovery tests pass; live signal delivery/restart is emulator validation only. |
| X3.6 | Crash dispatch/coexistence state machine | X3.3;X3.4;X3.5 | Exactly one primary/fallback result | PASS |  | The selected coexistence policy is handed through Kotlin/JNI into the native signal installation and dispatch path. Exclusive, best-effort prior-action chaining, conflict-disable, Crashpad failure, emergency fallback, and default termination produce one bounded primary/fallback result; a returning prior action is invoked at most once before forced default death. Host core/native policy and dispatch tests pass; installed-handler coexistence is emulator validation only. |
| X3.7 | JVM uncaught exception capture | C1.2;R2.5 | Bounded JVM capture and exact chaining | PASS |  | `Tracebox.install` installs the bounded no-message-default uncaught handler, preserves exact prior-handler chaining, contains recursion/cycles/failures, and writes through the generated policy-gated segment path. A fixed eight-record memory buffer covers only the early enabled-policy/sink-rotation window and discards on disable or overflow. Host installation, buffer, policy denial, persistence, and chaining tests pass; an actual ART crash/restart is emulator validation only. |
| X3.8 | Rust panic/fault integration | C1.5;X3.6 | Contained panic and native fault paths | PASS |  | The bounded Rust hook and cfg-gated unwind containment feed the versioned C bridge, native emergency slot, and fixed 64-record process ring. Production Android/JNI installs the hook and drains healthy client/handler rings into generated policy-gated records at startup and lifecycle triggers; disconnects fence capture. Cargo, ABI, native bridge, bounded-ring, abort-strategy, ingestion, and drain tests pass; Android panic/fault delivery is emulator validation only. |
| X3.9 | Live ANR watchdog | F0.6;X3.3;R2.1;R2.5 | Adaptive bounded watchdog | PASS |  | Production installation starts the Android main-looper watchdog and drives eligibility from activity visibility. The synchronized state machine uses bounded multi-sample stacks, positive retry delays, debugger/suspend/startup/rate-limit suppression, no ineligible heartbeat, optional bounded nonfatal request, and generated candidate persistence without claiming OS confirmation. Host state-machine, interleaving, lifecycle, timeout, and false-positive controls pass; latency/false-positive measurement is emulator validation only. |
| X3.10 | Exit reconciliation | C1.6;R2.4;R2.5;R2.8;X3.6;X3.7;X3.8;X3.9 | Policy-safe idempotent OS exit import | PASS |  | The API-30 adapter maps documented `ApplicationExitInfo` ANR fields, publishes and verifies the bounded process-role/epoch policy token before any raw read, streams raw bytes under a hard cap, and imports through exact source tombstones plus a crash-safe PREPARED/APPENDED journal. Journal entries are exact-size reads, the ledger is capped at 64 KiB, exhaustion never evicts, and restart replay is idempotent. Host adapter/stream/key/token/journal/corruption tests pass; actual OS history and trace availability are emulator validation only. API 37 `AnrInfo` remains advisory. |
| P4.1 | Snapshot selection and transformation | X3.2;R2.4;R2.5;R2.8;X3.10 | Frozen transformed snapshot | PASS |  | `OrdinarySourceRecord` accepts only bounded `GeneratedRecord` values and derives its payload internally; `RawArtifactSource` has no public byte accessor. UID quota check/insert, release, and usage queries are serialized, so concurrent exact-byte lease reservations cannot exceed a hard bound. `RecoveredSnapshotRequestAdapter` reads recovered Phase 2 frames by process role/cutoff and consumes the generated-record codec used by Phase 3 imported structural summaries. Evidence: `evidence/final/p4-recovered-store-adapter.json`. |
| P4.2 | Deterministic CBOR manifest | P4.1 | Canonical manifest vectors | PASS |  | RFC 8949 deterministic Kotlin and Rust encoders parse the shared fixture; the Rust test reruns the Kotlin test encoder and compares actual emitted byte arrays. Evidence: `evidence/phase4/review-fix-round2-host-validation.json`. |
| P4.3 | Constrained deterministic ZIP materialization | P4.1;P4.2 | Bounded deterministic tbdiag bytes | PASS |  | STORED deterministic ZIP, digest, malicious/boundary corpus, and actual-pipeline preview/approval digest fitness test pass on host. Evidence: `evidence/phase4/package-workflow-host-validation.json`. |
| P4.4 | Disclosure UI and approval | P4.3 | Exact decode and opaque approval | PASS |  | The sole concrete approval type is non-public and created by the Activity's private button-confirmation state machine; finalized package/manifest APIs are constructor-free interfaces. `javap -p` verification is recorded. Evidence: `evidence/phase4/review-fix-host-validation.json`. |
| P4.5 | FileProvider and Sharesheet | P4.4 | Read-only local handoff | PASS |  | Non-exported narrow staging `ContentProvider`, read-only URI grant/ClipData/Sharesheet, and TTL cleanup remain host-tested. Each lease independently reserves exact finalized bytes before writing; registration and expiry cleanup are serialized, and cleanup releases each lease exactly once. Evidence: `evidence/phase4/review-fix-round3-host-validation.json`. |
| P4.6 | SAF save | P4.4 | Cancellable local document copy | PASS |  | `ACTION_CREATE_DOCUMENT`, finalized-byte-only background copy, progress/cancellation, and typed partial-copy warning are host-tested. Evidence: `evidence/phase4/package-workflow-host-validation.json`. |
| P4.7 | Final receipt integration | P4.5;P4.6 | Accurate observable outcome receipt | PASS |  | Sealed approved receipt subvariants make failure/cancellation handoff unrepresentable: only `SaveSucceededPendingOrCompleteHandoff` has both `SaveResult.Complete` and a handoff. Evidence: `evidence/phase4/review-fix-round2-host-validation.json`. |
| T5.1 | Bounded Rust parser/validator | P4.2;P4.3 | Fuzzed streaming parser | PASS |  | Bounded STORED-v1 parser validates before allocating entry payloads, rejects traversal/duplicates/ZIP comments/extras/unsupported compression, and mutation/truncation smoke tests prove no panic. Evidence: `evidence/phase5/cli-parser-and-symbolication.json`. |
| T5.2 | Gradle build/symbol plugin | C1.6 | Build identity and conformance plugin | PASS |  | Public AGP variant tasks capture the exact application/SDK/schema/R8/ELF/Rust/Crashpad/dependency-verification/dependency-lock tuple. Release conformance consumes the merged manifest, rejects SDK or network-permission drift and forbidden locked runtime dependencies, and requires the current lock and verification hashes to match the generated identity exactly. Gradle unit/TestKit coverage and the real `noInternetQualificationRelease` conformance task pass. |
| T5.3 | Offline retrace/symbolication | T5.1;T5.2 | Exact-match offline symbols | PASS |  | The strict v2 catalog requires one complete build row, carries generated R8 and ELF/Rust entries, normalizes runtime native module basenames, enforces the full build tuple, retains unresolved raw frames, and rejects distinct alias matches as ambiguous rather than guessing. Cargo parser/CLI tests, malicious catalogs, Gradle real-artifact tests, and a real runtime-path catalog lookup pass; the emulator symbol smoke is validation only. |
| T5.4 | No-network conformance | ALL_FOUNDATION | Static, manifest, native, CLI and runtime proof | IN_PROGRESS |  | Repeatable host-static scan reads the generated release merged manifest and locked Android runtime closures. DEX/native-import scanning and blocked-egress runtime proof on the required existing emulator remain pending. Evidence: `evidence/phase5/review-fix-round4-host-validation.json`. |
| T5.5 | Personal-project resource qualification | R2.*;X3.*;P4.* | Architectural invariants plus one-emulator resource baseline | IN_PROGRESS |  | Record one useful API 36 emulator baseline for memory, idle CPU/wakeups, startup/readiness, ANR target pause, capture latency, package memory, and artifact size. Numerical percentile budgets, battery studies, physical devices, and OEM comparisons are advisory under ADR-0010. |
| T5.6 | Personal release readiness | ALL_PREVIOUS | Host gates, one-emulator suite, current documentation, and final review | IN_PROGRESS |  | `PERSONAL_RELEASE_READY` requires complete implementation, host/static gates, the existing API 36 x86_64 4 KiB emulator suite, and final approval. Tracker at `G:\Github\Tracker-Android` is a downstream evaluation host after `PERSONAL_RELEASE_READY`; physical/OEM lanes do not block. |
| E6.1 | C1/C2 segment AEAD | STABLE_STORAGE;SEPARATE_AUTHORIZATION | Gated post-foundation encryption | NOT_APPLICABLE_WITH_RATIONALE |  | Outside foundation and requires separate authorization. |
| E6.2 | Age X25519 | STABLE_PACKAGE;SEPARATE_AUTHORIZATION | Gated recipient encryption | NOT_APPLICABLE_WITH_RATIONALE |  | Outside foundation and requires separate authorization. |
| E6.3 | Metrics and traces | STABLE_SCHEMA;STABLE_PERFORMANCE | Out of foundation scope | NOT_APPLICABLE_WITH_RATIONALE |  | Outside foundation and requires separate authorization. |

## Ready-work rule

Ordinarily, a package is ready only when every dependency is `PASS`. ADR-0008
permits Phase 1-5 implementation despite historical Phase 0 failures. ADR-0009
requires only the existing API 36 x86_64 4 KiB emulator. ADR-0010 makes
physical/OEM breadth, percentile budgets, full-APK byte reproducibility,
eleven separate fixture applications, and sentence-level traceability
advisory. Historical evidence is retained but only current mandatory
implementation, host/static gates, and the one emulator lane determine
personal-release readiness.

## Explicit gate disposition

| Gate | Measured state | Decision | Effect |
|---|---|---|---|
| `ENGINEERING_FEASIBILITY_PASS` | NOT REACHED | Explicitly superseded for implementation by ADR-0008 | Phases 1–5 may proceed at risk; Phase 0 results remain unchanged. |
| `PERSONAL_RELEASE_FEASIBILITY_PASS` | NOT REACHED | Defined by ADR-0010 | Requires the consolidated functional/privacy/no-network/resource smoke on the single emulator. |
| `PERSONAL_RELEASE_READY` | NOT REACHED | Defined by ADR-0010 | Requires complete implementation, host/static PASS, required emulator PASS, current docs, and final review approval. |
| `FOUNDATION_CERTIFIED` | HISTORICAL_TERM | Superseded for personal release by ADR-0010 | No independent, physical-device, OEM, or broad-matrix certification claim is permitted. |

## Resume protocol

1. Verify branch, worktree, baseline ancestry, and cleanliness.
2. Select the first `IN_PROGRESS` package, otherwise the first dependency-ready `NOT_STARTED` package. Under ADR-0008, failed Phase 0 feasibility dependencies are explicit implementation risks rather than a stop condition for Phases 1–5; no other dependency is waived.
3. Read its traceability rows and frozen protocols before changing implementation.
4. Preserve structured evidence under `evidence/` and update rows only after verified commands finish successfully.
5. Commit coherent dependency gates without amending history.
